import { RegisterCard } from '@/components/RegisterCard'

/**
 * 新規登録ページ
 *
 * @remarks
 * メール/パスワードでの新規登録をサポート。
 * バリデーション、Firebase Authentication、API呼び出しを実行。
 */
export default function RegisterPage() {
  return <RegisterCard />
}
