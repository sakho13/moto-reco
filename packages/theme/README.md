# @packages/theme

モノレポ全体のテーマ設定を管理するパッケージ。

## 概要

このパッケージでは次のスタイルを管理します:

- カラー
- フォントサイズ
- 曲線
- シャドウ

## インストール

`package.json` に依存関係を追加:

```json
{
  "dependencies": {
    "@packages/theme": "workspace:*"
  }
}
```

## 使用方法
