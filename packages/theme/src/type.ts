export type ThemeTokens = {
  colors: {
    background: string
    cloud: string
    cloudHover: string
    // cloudActive: string

    // ブランドカラー
    product: string
    productHover: string
    productActive: string

    success: string
    danger: string
    warning: string
    social: string

    // テキスト文字色
    ink: string
    inkLight: string
    inkDark: string
  }
  fontSizes: {
    xs: string
    sm: string
    md: string
    lg: string
    xl: string
  }
  radius: {
    sm: string
    md: string
    lg: string
  }
  shadows: {
    sm: string
    md: string
    lg: string
  }
}
